# factory_calib GUI 使用指南（图像采集 tab）

> 适用：`build_fc_all\gui_qt\Release\factory_calib_gui.exe`（须从工程根 `E:\workfold\factory_calib` 启动，相对路径 `data_in/` `data_out/` 在此）

---

## 0. 启动

```powershell
cd E:\workfold\factory_calib
.\run_gui.bat
# 或
.\build_fc_all\gui_qt\Release\factory_calib_gui.exe
```

启动后**自动**：打开扫描仪串口 + 打开两台 Galaxy 相机（状态栏会提示）。
- 串口/相机打开失败会弹窗提示，检查 USB 连接 / 是否被其他软件占用。

---

## 1. 启动扫描仪 → 实时预览

1. 调整滑块（首次用默认值即可）：
   - **频率**：电机/扫描频率（默认 50）
   - **背光**：补光灯亮度（默认 40）
   - **激光**：激光亮度（默认 60）
   - **曝光**：相机曝光 µs（默认 10000）
2. 选模式：
   - **仅补光（相机标定）**：激光关，采棋盘格图用
   - **激光+补光（激光标定）**：激光开，采激光线图用
3. 点 **「▶ 启动扫描仪」**
   - 发 `N10` 启动扫描仪（电机+激光/补光）
   - 相机进入 Line2 硬件触发采集
   - **默认进入「连续预览」**：左右预览区实时刷新图像

> 预览是事件驱动（相机回调），非定时器轮询。照搬 LeadScanK2 机制，多次启停稳定。

---

## 2. 预览控制（两个按钮）

| 按钮 | 作用 |
|---|---|
| **「▶ 连续预览」**（checkable，默认勾选） | 界面实时刷新最新帧 |
| **「⏸ 预览当前帧」** | 界面**停在当前帧**不再刷新；后台相机仍持续刷新（不影响扫描仪） |
| **「⏺ 连续存储」**（checkable，默认不勾） | 点一下开始**连续存盘**（按钮变绿），再点一下停止。存的是后台抓取的**全分辨率左右成对帧**，与预览状态无关（冻结预览时也在存） |

典型用法：
- 扫描仪转动时连续预览，观察激光线/棋盘格姿态
- 看到满意的画面 → 点「⏸ 预览当前帧」停住
- 点「▶ 连续预览」恢复刷新

### 连续存储说明

- 存储目录：`data_in/continuous/<开始存储时间>/left/000000.png` + `right/000000.png`（编号自增，目录名=点开始那一刻的 `yyyyMMdd_HHmmss`）
- 需先「打开设备」按钮才可用；无帧到达时点了也只是建空目录
- 写盘在后台线程进行，不影响预览/采集；若写盘跟不上帧率会丢新帧（保内存），停止时状态栏汇总「已写 N 对，丢弃 M 对」
- 关设备/关窗口会自动停止存储并收尾

---

## 3. 保存图像（保存的是「当前显示帧」）

> 关键：保存的是**界面上当前显示的那一帧**（不是后台最新帧）。所以「预览当前帧」停住后保存，存的就是你看到的那张。

| 按钮 | 保存路径 | 用途 |
|---|---|---|
| **「💾 保存到 data_in/camera」** | `data_in/camera/left/000.png` + `right/000.png`（编号自增） | 相机标定（棋盘格图） |
| **「💾 保存到 data_in/laser/pose_NN」** | `data_in/laser/pose_NN/L_tube0.png` + `R_tube0.png` + `temp.txt`（NN 自增） | 激光标定（标定管激光线图） |

- 两个保存按钮在有显示帧后才可点
- 保存计数显示在底部（`相机: N 张    激光: N pose`）

---

## 4. 图像旋转

- **「左 180°」「右 180°」复选框：勾选则对应相机图像旋转 180°（适配物理倒装）
- 默认：左不转、右转（与参考软件 LeadScanK2 一致）

---

## 5. 停止 / 关闭

1. 点 **「■ 停止扫描仪」** → 发 `N11` 停电机 + 停相机采集 + 停预览
2. 点 **「关闭设备」** → 关两台相机
3. 点 **「关闭串口」** → 释放 COM 口
4. 关窗口即可

---

## 6. 采集完之后

图像存到 `data_in/` 后，去跑标定（命令行）：
- 相机标定：`build_fc_all\module1_camera\Release\camera_calib.exe data_in\camera data_out\camera_calib.json`
- 激光标定：先 `Copy-Item data_out\camera_calib.json data_in\laser\`，再 `build_fc_all\module2_laser\Release\laser_calib.exe data_in\laser data_out\laser_calib.json`

详见 `操作指南.md`。

---

## 常见问题

| 现象 | 原因/处理 |
|---|---|
| 点启动扫描仪不出图 | 扫描仪 USB/串口没连好；或被其他软件（LeadScanK2）占用相机 → 关掉那个软件 |
| 预览全黑 | 曝光太短（调到 10000µs+）；激光/背光没开；镜头盖没揭 |
| 保存按钮灰色 | 还没有显示帧，先启动扫描仪出图 |
| 保存的图不是看到的那帧 | 先点「⏸ 预览当前帧」停住再保存；连续预览中保存的是保存瞬间的最新帧 |
| 启动后偶发不出图 | 别在「启动」按钮里连发停止+启动；要停再启请分别点「■ 停止」「▶ 启动」（秒级间隔）|
