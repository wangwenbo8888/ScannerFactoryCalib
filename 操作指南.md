# 厂家标定操作指南

适用：`E:\workfold\factory_calib` 工程的 GUI（`factory_calib_gui.exe`）+ CLI（`camera_calib.exe` / `laser_calib.exe`）。

---

## 0. 准备

### 0.1 硬件连接

| 设备 | 接口 | 用途 |
|---|---|---|
| 扫描仪（含 2 台 Galaxy 工业相机） | USB3 | 立体图像采集 |
| 扫描仪主机控制板 | USB 转串口 (COM) | 启停扫描仪电机/激光 |
| 棋盘格标定板 | — | 相机内外参标定 |
| 标定管（带激光线） | — | 激光虚拟相机标定 |

确认：
- 扫描仪已上电、相机 USB 已插、串口 USB 已插
- Windows 设备管理器能看到 1 个 COM 口 + 2 台 Galaxy 相机

### 0.2 启动 GUI

```powershell
cd E:\workfold\factory_calib
.\build_fc_all\gui_qt\Release\factory_calib_gui.exe
```

或双击 `run_gui.bat`。

GUI 默认 `data_in/` `data_out/` 工作目录在工程根（必须从工程根启动，否则相对路径错）。

---

## 1. 采集相机标定图像（棋盘格）

### 1.1 准备 `data_in/camera/config.json`

```json
{
  "chessboard": {
    "width": 11,
    "height": 8,
    "square_size_mm": 25.0
  },
  "image_size": [2048, 1536],
  "temperature": {
    "cte": 23.6e-6,
    "referenceTemp": 25.0,
    "tempRangeMin": -10.0,
    "tempRangeMax": 10.0,
    "tempStep": 0.2
  },
  "rectify": { "alpha": 0.0, "flags": 1 }
}
```

字段说明：
- `chessboard.width / height` —— 棋盘格**内角点**数（不是格子数！11×8 表示 12×9 个方格）
- `chessboard.square_size_mm` —— 单格边长（毫米），用游标卡尺量准
- `image_size` —— 相机分辨率
- `temperature` —— 温度补偿参数；`referenceTemp` 必须与扫描仪实测参考温度一致
- `rectify` —— 立体矫正参数；`alpha=0` 是有效像素全保留，`flags=1` 即 `cv::CALIB_ZERO_DISPARITY`

### 1.2 准备 `data_in/camera/temps.txt`

```
ref_temp 25.0
```

> 单行 `ref_temp <摄氏度>`。文件优先级高于 `config.json` 的 `referenceTemp`。

### 1.3 GUI 操作流程

切到 **「图像采集」** tab：

1. **源类型** 下拉选 `galaxy`
2. 点 **刷新设备列表** → 看到两台 Galaxy 相机名（如 `[0] SN: XXX` `[1] SN: YYY`）
3. **扫描仪 COM** 下拉选扫描仪对应的 COM 口 → 点 **打开串口** → 状态栏显示"扫描仪串口 COMx 已打开"
4. 调整滑块（首次用默认值即可）：
   - 频率：50
   - 背光：40
   - 激光：60
5. 点 **▶ 启动扫描仪** → 状态栏显示"发送启动命令: N10 H50 B40 T1 V2 L60;"，扫描仪电机+激光开始转
6. **左源 ID** = `0`、**右源 ID** = `1` → 点 **打开设备** → 状态栏显示"设备已打开"，左右信息条显示分辨率
7. 调整 **曝光**（默认 10000 µs，棋盘格场景通常 8000~20000 µs）
8. 把棋盘格放到扫描仪视野中央，**铺满 ~60% 画面**
9. 点 **📸 采集一帧**：
   - 状态栏显示"已采集并保存一帧（WxH）到 data_in/camera/"
   - 左右预览区显示图像
   - 自动保存到 `data_in/camera/left/0.png` 和 `right/0.png`
10. **变换棋盘格姿态**（不同俯仰角、偏航角、距离）后再次点 **📸 采集一帧**
11. 重复 10~20 次，覆盖不同角度/位置，**避免完全平铺或单一姿态**

> 姿态建议：每张图棋盘格相对相机成 ±15°~30° 的俯仰/偏航；覆盖画面 5 个区域（中央 + 四角）；至少 10 张才够解算 11×8 棋盘。

### 1.4 检查采集结果

```powershell
# 工程根
Get-ChildItem data_in\camera\left  | Measure-Object  # 张数
Get-ChildItem data_in\camera\right | Measure-Object
```

左/右张数必须相等，每张 `.png` 大于 500 KB（2048×1536 单通道约 3 MB）。

### 1.5 停止扫描仪

采集结束后：
1. 点 **■ 停止扫描仪** → 发 `N11 H0;` 停电机
2. 点 **关闭串口** 释放 COM
3. 点 **关闭设备** 关相机

---

## 2. 跑相机内外参标定

### 2.1 命令行运行

```powershell
cd E:\workfold\factory_calib
.\build_fc_all\module1_camera\Release\camera_calib.exe `
    data_in\camera data_out\camera_calib.json
```

### 2.2 关键日志

正常输出（spdlog）：
```
loaded 15 frame pairs from data_in/camera
chessboard corners: left=15/15 right=15/15
intrinsic calib L rms=0.23  R rms=0.21
extrinsic stereo rms=0.18
rectify validRoi L=[...]
intrinsic temp table L: 101 entries
...
camera_calib -> data_out\camera_calib.json (status=ok)
```

### 2.3 检查输出

```powershell
# 输出文件
Test-Path data_out\camera_calib.json

# 字段速查
$json = Get-Content data_out\camera_calib.json -Raw | ConvertFrom-Json
$json.status             # ok / partial
$json.intrinsic.left.rms # 左相机内参 RMS（<0.5 像素为佳）
$json.intrinsic.right.rms
$json.extrinsic.rms      # 立体外参 RMS
```

### 2.4 退出码

| 退出码 | 含义 |
|---|---|
| 0 | 完整流水线成功（`status=ok`）|
| 1 | 部分失败（`status=partial`），具体看日志 |

### 2.5 重新采集

如果 RMS 太高（>1 像素）或角点检测失败：
1. 删 `data_in/camera/left/*.png` 和 `right/*.png`
2. 重新采集（**操作步骤同 §1.3**）
3. 重跑 §2.1

---

## 3. 采集激光线图像

### 3.1 准备 `data_in/laser/config.json`

```json
{
  "deviceId": 0,
  "plane_map": {
    "gridStep": 1.0,
    "depthMin": 50.0,
    "depthMax": 500.0,
    "depthSamples": 10,
    "epipolarStep": 1.0
  },
  "lineIds": [0, 1],
  "temperature": {
    "cte": 23.6e-6,
    "referenceTemp": 25.0,
    "tempRangeMin": -10.0,
    "tempRangeMax": 10.0,
    "tempStep": 0.2
  },
  "rectify": { "alpha": 0.0, "flags": 1 }
}
```

字段说明：
- `lineIds` —— 标定管上的激光线编号，**必须显式提供**（避免依赖运行时反推）
- `plane_map` —— 4-13 平面映射表参数
- `temperature` / `rectify` —— **必须与 §1.1 的 `camera/config.json` 完全一致**，否则 `validateHandoffConsistency` 拒绝

### 3.2 拷贝相机标定结果

```powershell
Copy-Item data_out\camera_calib.json data_in\laser\camera_calib.json
```

> 模块2 需要 `camera_calib.json` 提供：内参 K/D、外参 R/T、立体矫正 R1/R2/P1/P2/Q、imageSize、参考温度等。**没有这个文件模块2 跑不起来。**

### 3.3 GUI 操作流程

继续在「图像采集」tab：

1. 把**标定管**装到扫描仪上，**激光线对准相机视野**
2. 确认扫描仪 COM 串口已打开 → **▶ 启动扫描仪** 让激光点亮
3. 调高**激光滑块**（激光线足够亮才能被 steger 算子提取，建议 ≥ 50）
4. 调低**背光滑块**（背景越暗，激光线对比度越高，建议 ≤ 10）
5. **调整标定管姿态**（每个 pose 对应一个不同的几何位置/角度）
6. 点 **📸 采集一帧**：
   - 自动保存到 `data_in/laser/pose_NN/L_tube0.png` + `R_tube0.png`
   - 同时写 `data_in/laser/pose_NN/temp.txt` 内容 `ref_temp 25.0`
7. **变换标定管姿态** 后再次点 **📸 采集一帧** → 自动建 `pose_01/`
8. 至少采集 **15~25 个 pose**（覆盖激光线在视野中的不同深度/角度）

> 当前的「采集一帧」按钮自动按"相机标定路径"保存（写到 `data_in/camera/`）。激光标定路径需要点 **💾 保存到 data_in/laser/pose_NN** 按钮单独触发 —— 每点一次建一个新 pose 目录。

### 3.4 检查采集结果

```powershell
Get-ChildItem data_in\laser -Directory | Where-Object Name -like 'pose_*'
# 期望: pose_00, pose_01, ... 每个 pose 目录里 L_tube0.png + R_tube0.png + temp.txt
```

### 3.5 停止扫描仪

同 §1.5。

---

## 4. 跑激光虚拟相机标定

### 4.1 运行（需 CUDA）

```powershell
cd E:\workfold\factory_calib
$env:PATH = 'C:\opencv-cuda-4.13.0\x64\vc17\bin;' + $env:PATH
.\build_fc_all\module2_laser\Release\laser_calib.exe `
    data_in\laser data_out\laser_calib.json
```

### 4.2 关键日志

```
=== laser_calib (build 6.2-e final) ===
poses=20, imageSize=2048x1536, referenceTemp=25.0
4-1 MaskExtractCUDA x2 (L/R) constructed
...
pose 0 tube 0: OK (matched=82, reconstructed=82, total_accum=82)
...
loop done: 20 ok, 0 skipped
4-9 OK: 40 endpoints, 2 lines
4-10 OK: virtualT=(...), 2 lines, fit_err=0.0123
4-11 OK: totalReprojErr=0.456
5-3 OK: virtual→L/R temp tables (101 entries each)
4-13 OK: 101 temp entries
laser_calib (6.2-e) -> data_out\laser_calib.json (status=ok, exit=0)
```

### 4.3 退出码

| 退出码 | 含义 |
|---|---|
| 0 | 完整流水线成功（virtual pose + 5-3 + 4-13 全过，`status=ok`）|
| 1 | 部分失败（`status=partial`）：任一阶段没数据 / `lineIds` 空 / 无 3D 点累积 |

### 4.4 常见 partial 原因

| 现象（日志关键词） | 排查 |
|---|---|
| `4-3 label failed` | 激光线太暗，调高激光滑块重采 |
| `4-7 match failed` | 左右激光线遮挡太多 / 姿态太偏 |
| `no 3D points accumulated` | 整批都失败 → 全部重采 |
| `4-11 pose_optimize failed` | pose 太少（<10）或姿态无变化 |
| `4-13 lineIds empty` | `config.json` 缺 `lineIds` 字段，补上 `[0, 1]` |
| `referenceTemp mismatch` | `laser/config.json` 和 `camera_calib.json` 的 `referenceTemp` 不一致 |

---

## 5. 完整流水线一键（PowerShell 脚本）

```powershell
# === 相机标定 ===
.\build_fc_all\module1_camera\Release\camera_calib.exe `
    data_in\camera data_out\camera_calib.json
if ($LASTEXITCODE -ne 0) { Write-Host "camera_calib 失败"; exit 1 }

# === 拷贝交接 ===
Copy-Item data_out\camera_calib.json data_in\laser\camera_calib.json -Force

# === 激光标定 ===
$env:PATH = 'C:\opencv-cuda-4.13.0\x64\vc17\bin;' + $env:PATH
.\build_fc_all\module2_laser\Release\laser_calib.exe `
    data_in\laser data_out\laser_calib.json
```

---

## 6. 常见问题

| 现象 | 原因 / 处理 |
|---|---|
| `refresh 后看不到 galaxy 设备` | USB 没插好；Galaxy SDK 没装；驱动冲突 |
| `打开设备 失败` | 设备已被其他进程（参考软件 LeadScanK2）占用 → 关掉那个软件 |
| `采集超时/失败` | 扫描仪没启动（没点 ▶ 启动扫描仪）；Line2 触发线缆没接 |
| `采集后预览全黑` | 曝光太短（调到 10000 µs 以上）；镜头盖没揭；激光没开 |
| `camera_calib RMS > 1` | 棋盘格图太少/姿态单一；重新采集 ≥15 张覆盖多角度 |
| `laser_calib: handoff inconsistent` | `camera_calib.json` 的 `referenceTemp/cte/tempStep/...` 与 `laser/config.json` 不一致 |
| `factory_calib_gui.exe 启动崩` | 缺 DLL；检查 build_fc_all\gui_qt\Release\ 下 Qt5*.dll / Gx*.dll 是否齐全 |
| `0xc0000374 崩溃` | Galaxy SDK 与 Qt 串口/Win RT 状态冲突；通常重打开设备就好 |

---

## 7. 文件清单（参考）

```
factory_calib/
├── data_in/
│   ├── camera/                    ← 模块1 输入（§1）
│   │   ├── config.json            ← 棋盘格 + 温度参数
│   │   ├── temps.txt              ← ref_temp <摄氏度>
│   │   ├── left/0.png 1.png ...   ← 棋盘格左图
│   │   └── right/0.png 1.png ...
│   └── laser/                     ← 模块2 输入（§3）
│       ├── config.json            ← 激光线 + plane_map + lineIds
│       ├── camera_calib.json      ← 拷贝自 data_out/camera_calib.json
│       ├── pose_00/
│       │   ├── L_tube0.png        ← 激光左图
│       │   ├── R_tube0.png
│       │   └── temp.txt
│       └── pose_01/ ...
└── data_out/
    ├── camera_calib.json          ← 模块1 输出（§2）
    └── laser_calib.json           ← 模块2 输出（§4）
```
