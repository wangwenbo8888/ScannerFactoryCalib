# 开发记录

记录 `factory_calib` 工程的开发变更。按日期倒序排列。

---

## 2026-08-24：steger w_sum 除零修复（真亚像素从未生效）＋ 插值 v2 重写 ＋ 行级合并

### 1. 重大根因修复：steger 卷积 kernel 的 w_sum 除零

**现象**（用户发现）：中心提取输出的坐标全部是整数（x/y 100% 落在 .0000）——"亚像素"从未生效。

**排查路径**（教科书级的分层定位）：
1. CSV 实测确认坐标全整数 → kernel 输出层有问题
2. Python 分离卷积复现 kernel 数学：点 (77,289) 应得 t=-0.20、px=77.110 亚像素
3. kernel 内加 printf：实测 t=-0.0000、法向 (0.707,0.707)——与基准完全不同
4. 再打印导数值：**ix=1.59e9、iy=1.96e9、ixy=1e17（垃圾）；ixx/iyy 正常**
5. 好坏链交叉比对锁定：`GaussianConvRowKernel/ColKernel` 的边界归一化 `sum / w_sum`——**反对称核 gx 的 Σw≈1.86e-17（理论恒 0）**，除以它把 Ix/Iy/Ixy 放大 1e17 倍；gxx 的 Σw=−0.0013 幸存（故 ixx/iyy 一直正常，流水线"看似能跑"）
6. 垃圾导数 → Hessian 法向错乱 → Taylor 修正 t 恒 0 → 所有点=整数像素坐标

**修复**：卷积 kernel 加 `normalize_wsum` 参数——仅平滑核 (g) 归一（边界裁剪补偿），导数核 (gx/gxx) 绝不除；10 处调用点按核名接线。

**验证**（三层）：
| 层 | 方法 | 结果 |
|---|---|---|
| 数学复现 | 分离卷积基准对照 | ix=-16.65、t=0.2003、px=76.833 逐位一致 |
| CSV 输出 | 刷新后统计 | 100% 非整数（真亚像素） |
| **合成真值** | 已知斜线（斜率 0.503/σ=1.2 剖面/3 灰度噪声）提取 | **垂直误差 RMS=0.078px、bias≈0.003px** |

**连锁收益**：4-11 cond 5.7e18 → **2.5e9（改善 9 个数量级）**；4-10/4-11 光心收敛到差 1.7mm（此前差 117mm）——此前悬置的"光心漂移/病态"根源就是伪亚像素。compute-sanitizer 0 errors。

### 2. 插值 kernel v2 重写（按五条设计目标）

旧 kernel（逐点对驱动）与设计目标不符（门禁过宽 10/6px、单点产出跳行、同行可重复）。重写为**逐极线×逐线选点**（`kernelRowInterp`）：

1. 极线上下必须都有点（开区间严格夹住）
2. 两点 |ΔX|<1.4px 且 |ΔY|<1.4px 且各自距极线 Y<0.7px（=步距）
3. 二分查找夹住极线的最近点对（y 有序流保证）
4. 极线步距可配（当前 0.7px）
5. 槽位=line×极线行号，每线每极线物理唯一

Execute 内置 host 预处理：**(line, floor(y)) 行级合并取均值**（每线每行唯一点）＋线分段表上传供二分。

### 3. 行级合并（中心线稳定的关键）

**为什么一行多个点**：steger 对线宽内每个"脊响应像素"独立估计（线宽 4~6px → 每行 3~5 个候选，x 差 <0.3px）。**为什么必须合并**：不合并则 4-7 哈希同键塞多点、留哪个随机 → 中心线跳变 → 点云截面厚 1.25mm。

**均值 vs 最强响应**（实测对比）：多候选行的候选极差稳定 1.88px——是锯齿相位的两端点，均值恰好回到几何中心；最强响应有偏（亮度峰值≠几何中心）且不消锯齿。**选均值**。

**效果**：截面 Z-std 1.25 → 0.22mm；平面 RMS → 0.38mm（当前，7.1 万点）。

### 4. 期间的关键认知（记录备查）

- **dx 正负交替锯齿不是 bug**：理想斜线＋整数格采样＋法向修正的数学必然（模拟复现：理想线也产生 +1.1/−0.1 交替）；点仍精确在线上（垂直距离 <0.01px）
- **预加高斯滤波无益**：steger 内部已是 σ=1.5 高斯框架；实测预滤波前后中心差 0.021px；当前误差源是视场边缘畸变标定残差＋视差量化，不是高频噪声
- **点云"变差"是精度真实化**：点数 10 万→7.1 万（真 Hessian 门禁拦掉噪声响应），0.38mm 是无系统偏置的独立测量；5 个 |r|>3mm 离群全在视场左缘（棋盘盲区）
- **行级合并适用条件**：每行与线相交一次（斜线成立；近水平线不适用——四类中"精细/深孔"需留意线方向）

### 5. 输出与调试资产

- 三阶段数据 Excel：`steger_vs_undist_3frames.xlsx`（原始+矫正 114.8 万行）、`interp_points_3frames.xlsx`（7.6 万行）、`interp_by_line_3frames.xlsx`（按线分列 y 升序）
- 调试图/CSV 全部限前 3 帧（debug_steger/undist/interp 三目录）
- 合并点云 `all_poses_points3d.asc`（7.1 万点）

### 遗留

1. CLI 侧 [aggr] 行级合并（矫正前坐标）与 4-6 内部（矫正后坐标）串联冗余——建议删 CLI 侧，只留矫正后那次
2. 视场左缘 5 个离群（畸变盲区）——根治需重采棋盘覆盖边缘
3. 精细/深孔类型未跑（近水平线时行级合并需换策略）

---

## 2026-08-23（二）：4-13 守卫修复＋steger 排序＋光心异常定位

（承接 08-23 六连坑之后）

### 4-13 PlaneMapCuda 崩溃修复（坑⑧）

**根因**：plane_map 默认 gridStep=0.5 → 每线 1260 万虚拟像素 → 候选缓冲需求数百 GB → GpuMat int 列数溢出 → 分配出小缓冲 → kernel 越界写 → illegal access 崩进程。
**修复**：config 默认 gridStep 0.5→8、depthSamples 200→64；`PlaneMapParams` 加 `estimateCandidateBufferGB` + 4GB 守卫（超限抛清晰异常）。
**结果**：4-13 全通（101 温度点 × ~21.5 万映射对/点），status=ok。

### steger 排序缺 y 次级键（坑⑨）

`SubpixelPointLabelComp` 只按 label 排 → 线内乱序 → 4-6 相邻点对插值吃到"线两端任意两点" → 插值 x 剧烈跳变（二阶差分 RMS 83~105px）。加 y 次级键后恢复 1.6~2.3px、插值点 7k→45k/帧。

### 期间修正的测量假象（方法论教训）

- debug 图伪彩色容差 40 时相邻线号（颜色距 ~36px）互相污染 → 多次"串线/异常"误判；tol=12 后消失
- "暗谷找线中心"方法本身 ±20px 误差 → "插值点跑出线外"虚惊
- 真实问题用数值层面验证（不依赖颜色识别）：undist vs interp 同行对照 median 0.00px

### 中心行编号改进（坑⑩，08-23 下午）

4-3 原按"中心列"编号（为垂直线设计）——25 条斜线只编到中间 13~15 条。改**中心行**扫描（斜线全部横穿中部水平行），按交点 X 左→右排序编号：L/R 满编 25 且物理对应。配套：mask_extract 补面积过滤 TODO + keepTopK（按面积留前 K 条，K=config lineIds 数）+ expectedLineCount 校验。

### 相机标定参数修正

- **棋盘真实边长 15.0mm**（原 config 2.0 为臆设值）：角点实测间距 4.32mm 一致但尺度全错。修正后基线 133.19mm、投影仪距离 433mm、cond 从 1e14 降到 2.7e6
- rectify alpha 0.0→1.0（全像素保留，矫正焦距 2994→1278，激光可用点 ×37）
- RATIONAL 8 系数畸变尝试失败（OpenCV 4.13 stereoCalibrate+FIX_INTRINSIC 与 8 系数断言冲突）已回退

---

## 2026-08-23：左斜激光标定真实数据六连坑修复（4-1~4-11+5-3 全通）

**输入**：`data_in/laser/left_skew/`（pose_16~27，12 对真实激光图）。

| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | 4-1~4-3 连通域=0 | mask 默认 erodeSize=5 把 2~6px 激光线整条腐蚀 | mask 参数进 config（默认 th=60/erode=1） |
| 2 | 4-7 matched=0（一） | max_disparity=500，实际视差 600~1400 | 视差范围进 config，默认 [0,2000] |
| 3 | matched=0（二） | epipolar_interp max_x_diff=1 为垂直线设计，斜线全拒 | interp 约束进 config（10/6） |
| 4 | matched=0（三，重叠对视差 54000+） | undistort 算子把 P2[0][3]=-fx·B 基线项当加性常量并入 R 路 x（归一化点当 Z=1） | CLI 喂算子前清零 P 平移列（与 cv::undistortPoints 一致） |
| 5 | 4-9 OOM（max line_id=1.15e9） | match/reconstruct 的 fid 通道偶发读出未初始化显存（float 位型，竞态；隔离单 pose 不复现） | CLI 线号净化（>255 丢弃）；根因待深挖 |
| 6 | 4-10 有效线=0（一） | 扁平累积把不同 pose 同号线段并成散点 | 4-9 逐 pose 执行＋线号按 pose 偏移 |
| 7 | 4-10 有效线=0（二） | kernelCollectEndpoints 端点 id 写顺序索引（非线号）→ 分组全单点；且 minPointsPerLine=3>2 端点 | kernel 改写所属线号；minPointsPerLine=2 |

**结果**：12/12 pose OK、3649 3D 点、163 线段、4-10 虚拟光心 T=(11.7, 3.4, 58.0)mm、4-11 投影光心与其互验一致（cond=1.6e17 偏大待真机多姿态验证）、5-3 温度表 101 条。

**遗留**：4-13 PlaneMapCuda kernel illegal memory access（老算子从未被真实数据跑过；崩溃带走进程致 JSON 仍为 partial 旧结果）。下一个会话专项修。

---

## 2026-08-22（六）：激光线四类型拆分（采集/标定）＋ 退出前停扫描仪

**背景**：激光线分四种情况——左斜/右斜/精细/深孔，采集与标定都要按类型分开；下位机协议尚未按类型实现，通信暂统一用现有「标志点+激光线」N10 命令。

**改动**：

1. `gui_qt/gui_common.h`：新增四种激光线类型表 `kLaserTypes`（key/中文名）＋ `laserTypeDir/laserTypeOutPath/laserTypeName` 辅助（采集与标定共用单一来源）
2. `AcquisitionTab`：
   - 模式下拉改五项：仅标志点（相机标定，L=0）／标志点+左斜／右斜／精细／深孔激光线（data 0~4，≥1 均 laser=滑块值，协议同现状）
   - 「保存激光帧」按钮文案/可用性跟随模式：按类型存 `data_in/laser/<key>/pose_NN`，四类型独立 pose 计数；相机标定模式下禁用
   - 计数栏显示四类型各自 pose 数
3. `LaserCalibTab`：
   - 顶部新增「激光线类型」选择器；切换类型自动更新输入目录（`data_in/laser/<key>`）、输出文件（`data_out/laser_calib_<key>.json`，**四个独立结果文件**）、运行按钮文案；类型持久化（QSettings）
   - 「导入 camera_calib.json」增强：拷贝到当前类型目录；若该目录缺 `config.json` 则自动生成（温度字段从 handoff 读取保证一致性校验通过；rectify.flags=1024）；支持覆盖旧文件
4. `AcquisitionTab::~AcquisitionTab`：析构最先停扫描仪（串口开且 N10 已发才发 N11）——覆盖所有退出路径

**模块2 不需要改**：`runLaserCalib(inputDir, outputPath)` 本就参数化，按类型传不同目录即可产出四个结果文件。关闭时备份逻辑（data_bak）递归覆盖 `laser/<type>/**`，自动兼容。

**注意**：旧的 `data_in/laser/pose_*`（根目录 32 个 pose）不再被类型化路径消费；如需复用请手动移入对应类型子目录。

### 补充（同日）：四类型逐类标定 + 旧数据标定 + 退出停机

- **退出前停扫描仪**：`~AcquisitionTab` 析构最先判断（串口开且 N10 已发才发 N11），覆盖所有退出路径
- **跨会话续采**：`initSnapCounters()` 启动时扫描已有目录取最大编号+1，四类型独立计数，不覆盖旧图
- **旧数据标定（相机/激光同款）**：勾选「使用旧数据标定」→ 选文件夹（可选备份根/camera 或 laser 一级/精确类型目录，`resolveOldData` 三级探测自动定位）；未勾＝强制本次采集路径。激光侧记住 laser 根，切类型自动重定位；点运行时二次解析纠错
- **运行前自动准备**：激光标定启动前检查该类型有 pose_* 图像，camera_calib.json/config.json 缺失自动补齐（`ensureHandoffFiles`）
- **下位机协议**：四类激光模式暂统一用现有「标志点+激光线」N10 命令（下位机未区分类型）

---

## 2026-08-22（五）：标定启动时自动停止扫描仪

**改动**（`gui_qt/AcquisitionTab.h/.cpp`、`CameraCalibTab.h/.cpp`、`LaserCalibTab.h/.cpp`、`MainWindow.cpp`）：

- AcquisitionTab 新增 `scannerRunning_` 状态跟踪（N10 成功后置 true）＋公开方法 `stopScannerIfRunning()`：串口开且扫描仪在运行才执行停止（N11＋停预览＋停相机采集），避免对未启动的扫描仪盲发 N11；触发后状态栏提示
- CameraCalibTab/LaserCalibTab 新增 `calibrationStarted()` 信号：相机标定任一步骤（含一键全流程）与激光标定启动时发出
- MainWindow 接线：`calibrationStarted → stopScannerIfRunning`（Qt 默认直连，标定启动瞬间同步停机）

---

## 2026-08-22（四）：关闭 GUI 时自动备份并清理 data_in 图像

**改动**（`gui_qt/MainWindow.h/.cpp`）：

- 新增 `closeEvent` → `backupDataIn()`：收集 `data_in/camera/` 与 `data_in/laser/` 下全部图像（png/jpg/bmp，含 pose_NN 子目录），复制到 `data_bak/<yyyyMMdd_HHmmss>/`（与 data_in 同级，保留相对路径结构）；**逐文件校验复制成功（含大小比对）后才删除 data_in 中图像**，任一失败立即中止且不清除（保数据优先）；目录结构原样保留，非图像文件（temps.txt/config.json）不动
- 无图像时静默跳过（不建空备份目录、不弹窗）
- 菜单「退出」从 `QApplication::quit()` 改为 `close()`（quit 绕过 closeEvent，备份不会执行）
- 弹窗告知备份位置与数量

**局限**：进程被强杀/崩溃不走 closeEvent，无法备份（正常点 X 或菜单退出均有效）。

---

## 2026-08-22（三）：GUI 标定完成后弹窗汇报精度

**改动**（`gui_qt/CameraCalibTab.cpp`）：一键全流程（step≥4）成功后弹 QMessageBox，内容：有效帧数、内参重投影均值（对 `reprojErrorThreshold` 判定）、左/右相机 RMS、立体 RMS（对 `阈值×100` 判定，与算子 `maxReprojError` 同式）、极线误差均值（对 0.05px 判定）；全部达标→Information「精度达标」，任一超限→Warning「质量降级，建议改善采集后重标」。弹窗同时显示结果文件保存路径。

**验证**：编译通过（Release）。当前真实数据下 reproj 0.039>0.012、epipolar 0.28>0.05 会弹 Warning，符合预期。

---

## 2026-08-22（二）：输出文件拆分「结果 / 过程」

**背景**：`camera_calib.json` 单文件 531KB，逐视角诊断数据（55 视角 rvecs/tvecs、per_view_errors、perViewEpipolarErrors、外参里与 intrinsic 重复的 K/D 副本）与下游真正消费的结果混在一起。

**改动**（结果/过程两文件，`module1` 三处写入点同步）：

- `calib_io.h/.cpp`：`buildCameraCalibJson` 改为结果视角（剔除 `intrinsic.{left,right}` 的 rvecs/tvecs/per_view_errors、`extrinsic` 的 perView* / message / camera_matrix_l/r 副本）；新增 `buildCameraCalibProcessJson`（过程数据 + 运行配置复述，schema `factory_calib.camera_calib_process.v1`）与 `deriveProcessPath`（`camera_calib.json → camera_calib_process.json`）
- `camera_calib_cli.cpp` / `calib_runner.cpp` / `gui_qt/CameraCalibTab.cpp`：结果写完再写过程文件；过程写失败仅 warn 不影响退出码
- `README.md`：模块1 输出约定改为两文件说明

**兼容性**：module2 `loadCameraCalibHandoff` 只读 K/D、R/T、R1R2P1P2Q、imageSize、cte/温度区间——全在结果文件，交接不受影响；`schema` 仍为 v1 不变。

**验证**：CLI 重跑真实数据 exit=0；结果 493KB（无过程字段残留、交接字段齐备、4×101 温度表完整）+ 过程 51KB（rvecs/tvecs 55 视角、config 复述）；module1 单测全绿。

---

## 2026-08-22（一）：修复真实数据标定三连崩

### 概述

用真实采集数据（`data_in/camera/` 55 对 2048×1536 暗曝光棋盘格图）跑相机标定，暴露并修复 3 个 bug，模块1 CLI 全流程跑通（exit=0，产出 `data_out/camera_calib.json`）。

### 1. 角点检测全部失败 → SB 检测器加经典兜底

**现象**：`camera_calib.exe` 对 `data_in/camera` 全部 55 帧 "corner extraction failed, skip"。

**排查**：Python OpenCV 5.0 相同参数（11×8 + EXHAUSTIVE|NORMALIZE）能检出 88 角点 → 用工程同款 F:/opencv4.13 编 C++ 探针复测：**OpenCV 4.13 的 `findChessboardCornersSB` 对该类图（欠曝 + 板上周边圆形标记点 + 局部过曝亮斑）全 flag 组合均失败**，eq/CLAHE/gamma 预处理也救不活；经典 `findChessboardCorners`（ADAPTIVE_THRESH|NORMALIZE_IMAGE）全帧成功，网格间距一致性 dStd/mean≈1.5%。属 OpenCV 4.13→5.0 的版本行为差异，Python 验证掩盖了问题。

**修复**（`module1_camera/chessboard_corner.cpp`）：SB 失败时兜底走经典检测器，亚像素精化仍统一走 `cornerSubPix`。

### 2. 外参算子构造即崩（0xC0000409）：makeExtrinParams 漏填 objectPoints

**现象**：角点修好后流程过内参（reproj_mean=0.039）进入外参即 fail-fast 崩溃（exit=-1073740791）。

**根因**：`camera_calib_cli.cpp` 与 `calib_runner.cpp` 的 `makeExtrinParams` 均未填 `objectPoints`（默认空），`ExtrinsicCalibCpu` 构造时 `validate()` 抛 `std::invalid_argument`，CLI 无捕获 → `std::terminate`。

**修复**：两处 makeExtrinParams 按内参算子同一约定生成物点（行主序 + `plateTempCoeff` 温度膨胀修正），保证内/外参尺度一致。

### 3. 立体矫正算子构造抛异常：rectifyFlags 默认值 1 非法

**现象**：修完外参后流程又崩在 step 4（异常信息被 fail-fast 吞掉，靠 CLI 加打点 + try/catch 才拿到）。

**根因**：`CameraCalibConfig::rectifyFlags` 默认 =1，但 `stereoRectify` 仅接受 0 或 `CALIB_ZERO_DISPARITY`(=1024)，`StereoRectifyCpuParams::validate()` 抛异常未被捕获。

**修复**（`module1_camera/calib_io.h`）：默认值改为 `cv::CALIB_ZERO_DISPARITY`；CLI step 4 加 try/catch 防御 + 各步骤补进度日志。

### 验证

- `camera_calib.exe data_in\camera data_out\camera_calib.json`：exit=0；55/55 帧有效；stereo RMS 0.1007px；R 正交（det=1.0）、基线 T≈17.5mm；4 张温度表各 101 条（-10~10℃/0.2 步）
- module1 全部单测绿（chessboard 7 / e2e 1 / intrinsic 22 / extrinsic 28 / rectify 20 / intrinsic_compensate 18 / rectify_temp_table 9；extrinsic_compensate 本就 0 用例）
- GUI 因进程占用未重链（fc1_* 静态库已更新），关闭 GUI 后 `cmake --build build_fc_all --config Release --target factory_calib_gui` 即可

### 涉及文件

| 文件 | 改动 |
|---|---|
| `module1_camera/chessboard_corner.cpp` | SB 失败兜底经典检测器 |
| `module1_camera/camera_calib_cli.cpp` | makeExtrinParams 补 objectPoints；step4 try/catch；补步骤日志 |
| `module1_camera/calib_runner.cpp` | makeExtrinParams 补 objectPoints（GUI 路径） |
| `module1_camera/calib_io.h` | rectifyFlags 默认 1 → CALIB_ZERO_DISPARITY |

### 遗留 / 建议

1. **采集曝光偏低**：图像中位数灰度仅 10~14，内参 reproj 0.039 超阈值 0.012（warning）、epipolar 0.28px 超阈值 0.05px（degraded）。建议采集棋盘格时提高曝光/增益后重新采集标定
2. **`data_in/camera/` 缺 `config.json`**：当前走默认参数（11×8、square 2.0mm）。若实际板规格不同，建 config.json 写明 `chessboard.width/height/square_size_mm`
3. `test_extrinsic_compensate_cpu.cpp` 注册 0 个用例（历史现状，未动）

---

## 2026-08-08

### 概述

本次开发完成 3 件事：① 把 `ProjectorJointCalib` 投影仪联合标定算子从 `09_operatorlib` 移植过来并接入激光标定流程；② 修复 GUI 关闭时 `0xC0000005` 崩溃；③ 推送到 GitHub 远程仓库。

---

### 1. 移植 ProjectorJointCalib 投影仪联合标定算子

**背景**：对比 `E:\workfold\20260808\09_operatorlib`（3DSCANNER 算子库）与 `factory_calib`，发现 `ProjectorJointCalib` 是激光标定链里 factory_calib 没有的新算子（编号 14，在 4-11 之后）。它用「投影仪光心 t + CMOS 发射曲线」模型，比 factory_calib 现有的 `PoseOptimize`（virtual camera 针孔模型）更精确（显式建模 CMOS 发射面弯曲）。

**移植**：
- 来源：`09_operatorlib/calibration/laser_calib/projector_joint_calib/`
- 目标：`module2_laser/operators/projector_joint_calib/`（3 文件：`.h` / `.cpp` / `tests/test_*.cpp`）
- 方式：逐字拷贝（遵守 `AGENTS.md` 照搬原则），换行符 LF→CRLF 与仓库一致
- 依赖复用：module2 的 `common/` 已含全部依赖头（`calib_types/scanner_api/version/calib_warmup_config`），符号兼容性逐一校验通过（`QualityFlag` 枚举值、`SCANNER_API`、`OperatorInfo`、`WarmupConfig` 齐备）
- Ceres 后端：factory_calib 无 Ceres 依赖，`BUILD_CERES` 未定义 → `#if defined(BUILD_CERES)` 自动跳过，手写 LM 后端可用，无需引入新依赖
- 构建集成：`module2_laser/CMakeLists.txt` 用 `GLOB_RECURSE` 扫描 `operators/`，拷入目录即自动编译入库（`fc2_ops`）+ 测试自动注册

**验证**：`ctest -R projector_joint_calib` 15 个用例全过（含精度断言、退化姿态检测、伪极小值检测）。

---

### 2. 接入激光标定流程（取代 4-11 PoseOptimize）

**决策**：采用方案 B（完全替换 4-11）。核心妥协——`ProjectorJointCalib` 设计上不优化旋转 R 和内参 K，故 **K/R 沿用 4-10 `VirtualCameraPose`**，**T 用新算法的 `projectorT`**。下游 5-3/4-13 仍消费 `virtualK/R/T`（K/R 来自 4-10，T 来自新算法），未改下游算子。

**改动**（`module2_laser/laser_calib_cli.cpp`）：
- 主循环增加 **per-pose 点云累积**（`posePoints`/`poseLineIds`）：`ProjectorJointCalib` 要求按姿态分组（每姿态一块平板），而原流程把所有点 flat 累积
- 4-11 块替换为 `ProjectorJointCalib` 调用：输入重组 `poses`，`f`/主点取自 `virtualK`，初值用 `virtualT`；输出 `projectorT` + `emissionCurve`
- `finalLineCurves` 移除，4-13 的 `lineIds` 推断改用累积的实际线号
- JSON 新增字段：`projectorT` / `emissionCurve{coeffs,discriminant,sampsonRms,pointCount}` / `projectorQualityFlag` / `projectorCondNumber` / `projectorSampsonRms`

**验证状态**：
- 编译通过（`laser_calib.exe` 生成）
- e2e 冒烟测试通过（流程不崩）
- ⚠️ **局限**：冒烟测试用全黑合成图，`host_points3d` 为空 → 4-9~4-13 全部跳过，`ProjectorJointCalib` 未被真正执行验证。真实端到端验证需 ≥5 个 pose 的真实激光数据 + `camera_calib.json` + `config.json`（产线样本待提供）

---

### 3. 修复 GUI 关闭时 0xC0000005 崩溃

**现象**：关闭 `factory_calib_gui.exe`（尤其跑过标定后）崩在 `Qt5Widgets.dll!0x52cb48b0`，读 `0x8`（访问已析构 widget 的 `d_ptr`）。关闭前无论是否跑标定都可能崩。

**真根因**：`MainWindow::buildCentral()` 的状态栏转发 lambda：
```cpp
auto fwd = [this](QString m) { statusBar()->showMessage(m, 5000); };
connect(acquisitionTab_, &AcquisitionTab::statusMessage, this, fwd);
```
`fwd` 捕获 `this` 调 `statusBar()`。关闭时 `~QMainWindow` 删除子对象顺序中，残留的 `statusMessage` 信号触发 `fwd`，此时 `statusBar()` 已析构 → 读空 `d_ptr` → 崩溃。

**修复**（`gui_qt/MainWindow.cpp`）：
- `fwd` 改用 `QPointer<MainWindow>` 捕获，析构后自动置 null，`if (self)` 守卫
- `~MainWindow` 显式 `tab->disconnect(this)` 断开连接（双保险）

**防御性加固**（虽非本崩溃主因，但是同类 race 的合理防护，一并修复）：
- `SpdlogBridge`：加 `destroyed_` 标志 + `sinkInFlight_` 计数；`uninstall()` 先标记、等待 in-flight `sink_it_` 退出、再摘除 sink。防析构期间后台线程 `invokeMethod(this)` 访问悬空对象
- `CalibWorker::wait()` + `~LaserCalibTab` 析构前 `worker_->wait()`：确保标定线程退出后才析构
- `GalaxyCameraSource`：`dispatchFrame` 加 in-flight 计数；`stopAcquisition` 在 `UnregisterCaptureCallback` 后等待 in-flight 回调退出
- `~AcquisitionTab`：改为正确停止顺序（清回调 → stop → disconnect → close）+ 修复 `m_pLeft/m_pRight` 裸指针泄漏

**排查教训**（重要，记入避免重蹈覆辙）：
- 调用栈无符号（Release 无 pdb）时，**不要凭地址推理改代码**——本次先后误判 Galaxy 相机回调、SpdlogBridge 为根因，改了两轮无效。正确做法：先启用符号（`/Zi /DEBUG`）拿到符号化调用栈，再定位
- `0xC0000005 读 0x8` 在 `Qt5Widgets.dll` = 典型的「访问已析构 QWidget 的 `d_ptr`」，应优先排查析构期间被触发的信号槽/事件

---

### 4. 启用 Release 调试符号

`gui_qt/CMakeLists.txt` 加 `/Zi`（编译）+ `/DEBUG`（链接），生成 `factory_calib_gui.pdb`。便于以后崩溃时用 `dbghelp`/VS 反查地址定位函数。

---

### 5. 推送到 GitHub 远程仓库

- 远程：`https://github.com/wangwenbo8888/ScannerFactoryCalib`
- GitHub 直连不通（`github.com:443` 连接失败），走 **`ghfast.top` 镜像** push 成功
- 3 个 commit：
  - `module2: 接入 ProjectorJointCalib 投影仪联合标定算子`
  - `gui: 修复关闭时 0xC0000005 崩溃（statusBar 析构 race + 防御加固）`
  - `gui: 启用 Release 调试符号 (/Zi /DEBUG)`
- 后续 push 若遇网络问题：`git push "https://ghfast.top/https://github.com/wangwenbo8888/ScannerFactoryCalib.git" master`

---

### 涉及文件清单

| 类别 | 文件 |
|---|---|
| 新增算子 | `module2_laser/operators/projector_joint_calib/{projector_joint_calib.h,.cpp,tests/test_projector_joint_calib.cpp}` |
| 接入流程 | `module2_laser/laser_calib_cli.cpp` |
| GUI 崩溃修复（主因）| `gui_qt/MainWindow.cpp` |
| GUI 防御加固 | `gui_qt/SpdlogBridge.h/.cpp`, `gui_qt/CalibWorker.h/.cpp`, `gui_qt/LaserCalibTab.cpp`, `gui_qt/AcquisitionTab.cpp`, `gui_qt/camera/GalaxyCameraSource.h/.cpp` |
| 构建配置 | `gui_qt/CMakeLists.txt` |

---

### 遗留问题 / 注意事项

1. **激光标定新算法未真实数据验证**：`ProjectorJointCalib` 在 factory_calib 内只过了自测 + 冒烟，真实端到端（多 pose 激光数据）精度未验证，待产线样本
2. **`CameraCalibTab` 析构未 wait future**：与 `LaserCalibTab` 同样的缺陷（局部 `QFutureWatcher` 捕获 `this`）。`SpdlogBridge` 加固已防住 spdlog 类崩溃，但相机标定的 `futureFn` 若在析构中访问 `this` 成员仍有理论风险，后续需补修
3. **Ceres 后端未启用**：`ProjectorJointCalib` 当前只能用手写 LM；若需 Ceres 后端，需先给 factory_calib 引入 Ceres 依赖
4. **GitHub 直连不通**：本环境需走 `ghfast.top` 镜像 push，`origin` 仍指向直连地址

---

## 开发环境

- 工程根：`E:\workfold\factory_calib`
- 构建：`build_fc_all`（VS2022 x64，`CMAKE_CONFIGURATION_TYPES=Release`）
- OpenCV：`F:/opencv4.13/install`（含 CUDA）
- 相机 SDK：Galaxy（`FC_HAVE_GALAXY=ON`）+ Hikvision MVS（`FC_HAVE_MVS=ON`），均启用
- Qt：5.9.3（`D:/QTInstall/5.9.3/...`）
